#include<bits/stdc++.h>
using namespace std;

int main(){
    int arr[5];
    cin>>arr[0]>>arr[1]>>arr[2]>>arr[3]>>arr[4];

    arr[3] +=10;

    cout<<arr[3];
    return 0;
}

/*
Everything should be of similar datatype.
Whatever datatype you give that has to be constant for all the elements .
*/

/*
Array elements are stored in consecutive memory address but i am not sure 
where the first one is it can be at any randomise location.
*/